# Runtime dependencies of the Authorship DB app (app.py).
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
sqlalchemy>=2.0
python-multipart>=0.0.9
passlib>=1.7.4
# passlib 1.7.4 mis-detects bcrypt >= 4.1 ("error reading bcrypt version");
# keep bcrypt at 4.0.x until passlib ships a fix.
bcrypt==4.0.1
